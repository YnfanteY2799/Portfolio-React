import { BtnProps } from "../../utils/types/Router-Types";

export default function Button({ title }: BtnProps) {
  return <button>{title}</button>;
}

// LATER ON https://v1.tailwindcss.com/components/buttons
